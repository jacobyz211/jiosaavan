{
  "name": "eclipse-jiosaavn-addon",
  "version": "1.1.0",
  "description": "Eclipse Music addon exposing JioSaavn search and streaming, cached via Upstash Redis",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
